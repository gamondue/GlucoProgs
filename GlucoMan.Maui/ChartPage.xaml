<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
             x:Class="GlucoMan.Maui.ChartPage"
             Title="Chart">
    <Grid Padding="15,10,15,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Placeholder content -->
        <Label x:Name="lblDataType" 
               Grid.Row="0"
               FontSize="18"
               HorizontalOptions="Center"/>
        
        <Label x:Name="lblDateRange" 
               Grid.Row="1"
               FontSize="16"
               HorizontalOptions="Center"
               TextColor="Gray"
               Margin="0,0,0,20"/>
        
        <!-- Date navigation buttons -->
        <Button x:Name="btnPreviousDay"
                Grid.Row="1"
                Text="◀"
                FontSize="20"
                HorizontalOptions="Start"
                VerticalOptions="Center"
                BackgroundColor="Transparent"
                TextColor="Black"
                Padding="10,0"
                Clicked="OnPreviousDayClicked"/>
        
        <Button x:Name="btnNextDay"
                Grid.Row="1"
                Text="▶"
                FontSize="20"
                HorizontalOptions="End"
                VerticalOptions="Center"
                BackgroundColor="Transparent"
                TextColor="Black"
                Padding="10,0"
                Clicked="OnNextDayClicked"/>
        
        <!-- Chart content - Using SKCanvasView directly for testing -->
        <skia:SKCanvasView x:Name="chartView"
                           Grid.Row="2"
                           PaintSurface="OnChartPaintSurface"
                           VerticalOptions="FillAndExpand"
                           HorizontalOptions="FillAndExpand"
                           />
    </Grid>
</ContentPage>
