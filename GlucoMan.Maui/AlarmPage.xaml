<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:strings="clr-namespace:GlucoMan.Maui.Resources.Strings"
             x:Class="GlucoMan.Maui.AlarmPage"
             Title="{x:Static strings:AppStrings.AlarmPageTitle}">
    <ContentPage.Resources>
        <!-- Colors / Brushes reused across fields -->
        <Color x:Key="EditBackColor">LightGreen</Color>
        <Color x:Key="ResultBackColor">#ADD8E6</Color> <!-- Light Azure -->
        <Style x:Key="EditEntryStyle" TargetType="Entry">
            <Setter Property="BackgroundColor" Value="{StaticResource EditBackColor}" />
            <Setter Property="TextColor" Value="Black" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="2" />
        </Style>
        <Style x:Key="ResultEntryStyle" TargetType="Entry">
            <Setter Property="BackgroundColor" Value="{StaticResource ResultBackColor}" />
            <Setter Property="TextColor" Value="Black" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="IsReadOnly" Value="True" />
        </Style>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Margin" Value="2,4,2,0" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="2" />
            <Setter Property="VerticalOptions" Value="Center" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="6,4" />
        </Style>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*">
        <ScrollView Grid.Row="0" HeightRequest="420" VerticalOptions="Start">
            <VerticalStackLayout Padding="6" Spacing="4">
            
            <!-- Windows-specific warning banner -->
            <Frame x:Name="frmWindowsWarning" 
                   IsVisible="False"
                   BackgroundColor="#FFF3CD" 
                   BorderColor="#FFC107" 
                   CornerRadius="6" 
                   Padding="12" 
                   Margin="0,0,0,8">
                <VerticalStackLayout Spacing="6">
                    <Label Text="⚠️ WINDOWS ALARM NOTICE" 
                           FontSize="16" 
                           FontAttributes="Bold" 
                           TextColor="#856404" />
                    <Label TextColor="#856404" FontSize="13">
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="Alarms on Windows require the app to be running. " />
                                <Span Text="Do NOT close the app " FontAttributes="Bold" />
                                <Span Text="if you have active alarms. You can minimize it instead."/>
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>
                    <Label Text="Tip: Keep GlucoMan minimized in the taskbar for reliable alarms." 
                           FontSize="12" 
                           FontAttributes="Italic" 
                           TextColor="#856404" />
                </VerticalStackLayout>
            </Frame>
            
            <!-- Current Alarm Editing Panel -->
            <Frame BorderColor="#888" CornerRadius="6" Padding="8" Margin="0,0,0,6">
                <Grid ColumnDefinitions="60,*,70,*" RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto">
                    <!-- Row 0: Id / Reminder -->
                    <Grid Grid.Row="0" Grid.ColumnSpan="4" ColumnDefinitions="20, 45, Auto, *">
                        <Label Grid.Column="0" VerticalOptions="Center" Text="{x:Static strings:AppStrings.Id}" />
                        <Entry Grid.Column="1" VerticalOptions="Center" x:Name="txtIdAlarm" 
                               Style="{StaticResource ResultEntryStyle}" />
                        <Label Grid.Column="2" VerticalOptions="Center" Text="Rem." />
                        <Entry Grid.Column="3" VerticalOptions="Center" x:Name="txtReminderText" 
                               Style="{StaticResource EditEntryStyle}" 
                               Placeholder="Reminder text" />
                    </Grid>
                    <!-- Row 1: TimeStart Date/Time -->
                    <Label Grid.Row="1" Grid.Column="0" Text="Start Date" />
                    <DatePicker x:Name="dtpStartDate" Grid.Row="1" Grid.Column="1" BackgroundColor="{StaticResource EditBackColor}" />
                    <Label Grid.Row="1" Grid.Column="2" Text="Start Time" />
                    <TimePicker x:Name="tpStartTime" Grid.Row="1" Grid.Column="3" BackgroundColor="{StaticResource EditBackColor}" />

                    <!-- Row 2: Trigger / Interval -->
                    <Label Grid.Row="2" Grid.Column="0" Text="Trigger (s)" />
                    <Entry x:Name="txtTriggerInterval" Grid.Row="2" Grid.Column="1" Style="{StaticResource EditEntryStyle}" Keyboard="Numeric" />
                    <Label Grid.Row="2" Grid.Column="2" Text="{x:Static strings:AppStrings.Repeat}" />
                    <Entry x:Name="txtInterval" Grid.Row="2" Grid.Column="3" Style="{StaticResource EditEntryStyle}" Keyboard="Numeric" />

                    <!-- Row 3: Duration / RepetitionTime -->
                    <Label Grid.Row="3" Grid.Column="0" Text="{x:Static strings:AppStrings.Duration}" />
                    <Entry x:Name="txtDuration" Grid.Row="3" Grid.Column="1" Style="{StaticResource EditEntryStyle}" Keyboard="Numeric" />
                    <Label Grid.Row="3" Grid.Column="2" Text="Repetition After (s)" />
                    <Entry x:Name="txtRepetitionTime" Grid.Row="3" Grid.Column="3" Style="{StaticResource EditEntryStyle}" Keyboard="Numeric" />

                    <!-- Row 4: Repeat Count / Max -->
                    <Label Grid.Row="4" Grid.Column="0" Text="Repeat Count" />
                    <Entry x:Name="txtRepeatCount" Grid.Row="4" Grid.Column="1" Style="{StaticResource ResultEntryStyle}" />
                    <Label Grid.Row="4" Grid.Column="2" Text="Max Repeats" />
                    <Entry x:Name="txtMaxRepeatCount" Grid.Row="4" Grid.Column="3" Style="{StaticResource EditEntryStyle}" Keyboard="Numeric" />

                    <!-- Row 5: Next / Last trigger -->
                    <Label Grid.Row="5" Grid.Column="0" Text="Next Trigger" />
                    <Entry x:Name="txtNextTriggerTime" Grid.Row="5" Grid.Column="1" Style="{StaticResource ResultEntryStyle}" />
                    <Label Grid.Row="5" Grid.Column="2" Text="Last Trigger" />
                    <Entry x:Name="txtLastTriggerTime" Grid.Row="5" Grid.Column="3" Style="{StaticResource ResultEntryStyle}" />

                    <!-- Row 6: Triggered Count / Playing -->
                    <Label Grid.Row="6" Grid.Column="0" Text="Triggered" />
                    <Entry x:Name="txtTriggeredCount" Grid.Row="6" Grid.Column="1" Style="{StaticResource ResultEntryStyle}" />
                    <Label Grid.Row="6" Grid.Column="2" Text="Playing" />
                    <CheckBox x:Name="chkPlaying" Grid.Row="6" Grid.Column="3" />

                    <!-- Row 7: Sound flags -->
                    <Label Grid.Row="7" Grid.Column="0" Text="Play Sound" />
                    <CheckBox x:Name="chkPlaySound" Grid.Row="7" Grid.Column="1" />
                    <Label Grid.Row="7" Grid.Column="2" Text="{x:Static strings:AppStrings.Vibrate}" />
                    <CheckBox x:Name="chkVibrate" Grid.Row="7" Grid.Column="3" />

                    <!-- Row 8: Sound file path -->
                    <Label Grid.Row="8" Grid.Column="0" Text="{x:Static strings:AppStrings.Sound}" />
                    <Entry x:Name="txtSoundFilePath" Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="3" Style="{StaticResource EditEntryStyle}" Placeholder="Path or filename" />

                    <!-- Row 9: Spacer or future state -->
                    <Label Grid.Row="9" Grid.Column="0" Text="{x:Static strings:AppStrings.Status}" />
                    <Entry x:Name="txtState" Grid.Row="9" Grid.Column="1" Style="{StaticResource ResultEntryStyle}" />
                    <Label Grid.Row="9" Grid.Column="2" Text="Reminder Preview" />
                    <Entry x:Name="txtReminderPreview" Grid.Row="9" Grid.Column="3" Style="{StaticResource ResultEntryStyle}" />
                </Grid>
            </Frame>

            <!-- Command Buttons -->
            <FlexLayout Direction="Row" Wrap="NoWrap" AlignItems="Center" JustifyContent="Start">
                <Button x:Name="btnAdd" Text="{x:Static strings:AppStrings.Add}" />
                <Button x:Name="btnSave" Text="{x:Static strings:AppStrings.Save}" />
                <Button x:Name="btnDelete" Text="{x:Static strings:AppStrings.Delete}" />
                <Button x:Name="btnDismiss" Text="Dismiss" />
                <Button x:Name="btnActivate" Text="Activate" />
                <Button x:Name="btnDeactivate" Text="Deactivate" />
                <Button x:Name="btnTest" Text="Test" />
                <Button x:Name="btnClear" Text="{x:Static strings:AppStrings.Clear}" />
            </FlexLayout>

            <!-- Filters Panel -->
            <Frame BorderColor="#888" CornerRadius="6" Padding="6" Margin="0,3,0,2">
                <Grid ColumnDefinitions="Auto,*,Auto,*,Auto,*,Auto,*" RowDefinitions="Auto,Auto">
                    <Label Grid.Row="0" Grid.Column="0" Text="{x:Static strings:AppStrings.From}" />
                    <DatePicker x:Name="dtpFrom" Grid.Row="0" Grid.Column="1" BackgroundColor="{StaticResource EditBackColor}" />
                    <Label Grid.Row="0" Grid.Column="2" Text="{x:Static strings:AppStrings.To}" />
                    <DatePicker x:Name="dtpTo" Grid.Row="0" Grid.Column="3" BackgroundColor="{StaticResource EditBackColor}" />
                    <CheckBox x:Name="chkShowAll" Grid.Row="0" Grid.Column="4" />
                    <Label Grid.Row="0" Grid.Column="5" Text="All" />
                    <CheckBox x:Name="chkActive" Grid.Row="0" Grid.Column="6" IsChecked="True" />
                    <Label Grid.Row="0" Grid.Column="7" Text="Active" />

                    <CheckBox x:Name="chkExpired" Grid.Row="1" Grid.Column="0" />
                    <Label Grid.Row="1" Grid.Column="1" Text="Expired" />
                    <Button x:Name="btnRefresh" Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Text="{x:Static strings:AppStrings.Refresh}" />
                    <Button x:Name="btnNow" Grid.Row="1" Grid.Column="4" Grid.ColumnSpan="2" Text="{x:Static strings:AppStrings.Now}" />
                    <Button x:Name="btnSetNext" Grid.Row="1" Grid.Column="6" Grid.ColumnSpan="2" Text="Set Next" />
                </Grid>
            </Frame>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Bottom: list area so CollectionView owns scrolling/touches -->
        <Grid Grid.Row="1" Padding="6">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <HorizontalStackLayout Grid.Row="0" Spacing="10">
                <Label Text="{x:Static strings:AppStrings.AlarmsList}" FontAttributes="Bold" />
                <Label x:Name="lblAlarmCount" Text="(0 alarms)" FontSize="12" TextColor="Gray" VerticalOptions="Center" />
            </HorizontalStackLayout>
            <!-- Fixed column header for the alarms list (does not scroll) -->
            <Grid Grid.Row="1" ColumnDefinitions="20*,40*,80*,80*,20*" ColumnSpacing="4" BackgroundColor="#ececec" Margin="0,6,0,0" Padding="4,6">
                <Label Text="{x:Static strings:AppStrings.Id}" FontAttributes="Bold" TextColor="Black" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                <Label Grid.Column="1" Text="Reminder" FontAttributes="Bold" TextColor="Black" HorizontalTextAlignment="Start" VerticalTextAlignment="Center" />
                <Label Grid.Column="2" Text="Start" FontAttributes="Bold" TextColor="Black" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                <Label Grid.Column="3" Text="Next" FontAttributes="Bold" TextColor="Black" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                <Label Grid.Column="4" Text="St" FontAttributes="Bold" TextColor="Black" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
            </Grid>
            <CollectionView Grid.Row="2" x:Name="cvAlarms" SelectionMode="Single" BackgroundColor="LightGray" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="2" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                <Frame CornerRadius="6" Padding="6" Margin="2,1" BackgroundColor="White" BorderColor="#DDD" HasShadow="True" HorizontalOptions="FillAndExpand">
                            <Frame.Triggers>
                                <DataTrigger TargetType="Frame" Binding="{Binding IsSelectedInList}" Value="True">
                                    <Setter Property="BorderColor" Value="DeepSkyBlue" />
                                    <Setter Property="BackgroundColor" Value="#e6f7ff" />
                                </DataTrigger>
                                <DataTrigger TargetType="Frame" Binding="{Binding IsSelectedInList}" Value="False">
                                    <Setter Property="BorderColor" Value="#DDD" />
                                    <Setter Property="BackgroundColor" Value="White" />
                                </DataTrigger>
                            </Frame.Triggers>
                            <Grid ColumnDefinitions="20*,40*,80*,80*,20*" ColumnSpacing="4">
                                <Label Text="{Binding IdAlarm}" TextColor="Black" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" FontSize="14" />
                                <Label Grid.Column="1" Text="{Binding ReminderText, TargetNullValue='(no reminder)'}" 
                                       TextColor="Black" LineBreakMode="TailTruncation" 
                                       VerticalTextAlignment="Center" 
                                       FontSize="14" HorizontalOptions="StartAndExpand" MaxLines="2" />
                                <Label Grid.Column="2" Text="{Binding TimeStart.DateTime}" TextColor="Black" 
                                       VerticalTextAlignment="Center" HorizontalTextAlignment="Center" />
                                <Label Grid.Column="3" Text="{Binding NextTriggerTime}" TextColor="Black" 
                                       VerticalTextAlignment="Center" HorizontalTextAlignment="Center" />
                                <Label Grid.Column="4" Text="{Binding TriggeredCount}" TextColor="Black" 
                                       VerticalTextAlignment="Center" HorizontalTextAlignment="Center" FontSize="14" />
                            </Grid>
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnItemTapped" NumberOfTapsRequired="1" />
                            </Frame.GestureRecognizers>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                <CollectionView.EmptyView>
                    <Label Text="No alarms to display" TextColor="Gray" HorizontalOptions="Center" VerticalOptions="Center" />
                </CollectionView.EmptyView>
            </CollectionView>
        </Grid>
    </Grid>
</ContentPage>
